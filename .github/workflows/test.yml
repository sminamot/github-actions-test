name: Test
on:
  push:
    branches:
      - develop
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
    - name: Echo
      run: |
        echo $GITHUB_REF
        echo ${{ github.repository }}
        branch=${GITHUB_REF##*/}
        echo $branch
        echo ::set-env name=branch::${branch}
        echo "==="
        echo $branch

    - name: Echo2
      run: |
        echo $branch
