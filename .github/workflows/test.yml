name: Test
on:
  push:
    tags:
      - develop
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
    - name: Echo
      run: |
        echo $GITHUB_REF
        echo ${{ github.repository }}
        BRANCH=${GITHUB_REF##*/}
        echo $BRANCH
        echo ::set-env name=BRANCH::${BRANCH}
        echo "==="
        echo $BRANCH

    - name: Echo2
      run: |
        echo $BRANCH
